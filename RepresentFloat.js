// LLVM IR 中 float 需要特殊表示

/**
 * 将传入的浮点数转换成 LLVM 支持的表示
 * @param {string} f 
 * @return {string}
 */
const representFloat = function(f) {
    //TODO 补充详细过程
    return f;
};

module.exports = {
    'representFloat': representFloat
};